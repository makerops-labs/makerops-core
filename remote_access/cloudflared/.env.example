# ─── cloudflared — Cloudflare Tunnel ──────────────────────────────────────────
# No secrets or routing config here — the tunnel's identity and ingress
# rules live in config/config.yml + config/credentials-*.json (gitignored,
# see README.md for how those get created). This file exists only for
# convention-consistency with every other service directory in this repo.

TZ=America/Chicago
